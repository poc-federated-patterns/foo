name: Call Bar (Demonstrates secrets inherit)

on:
    workflow_dispatch:

jobs:
    call-bar:
        uses: poc-federated-patterns/bar/.github/workflows/reusable-internal.yml@main
        secrets: inherit
        # Note: This passes FOO's secrets to bar, NOT bar's own secrets
